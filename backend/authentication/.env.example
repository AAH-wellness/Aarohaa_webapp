# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Configuration
# ============================================
# SUPABASE SETUP:
# 1. Go to https://app.supabase.com → Your Project → Settings → Database
# 2. Copy the "Connection string" (URI format)
# 3. Replace [YOUR-PASSWORD] with your actual database password
# 4. Uncomment and use the DATABASE_URL line below
# ============================================

# Option 1: Use connection string (for Supabase or cloud databases)
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
# DB_SSL=true

# Option 2: Use individual parameters (for local PostgreSQL or Supabase)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aarohaa_db
DB_USER=postgres
DB_PASSWORD=your-database-password-here
DB_SSL=false

# JWT Configuration
# Generate a secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=2h

# Google OAuth Configuration
# Replace with your actual Google OAuth Client ID from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
